version: '3.7'

services:

  frontend:
    restart: always
    container_name: frontend
    build:
      context: ./frontend
    ports:
      - 8080:8080
    expose:
      - 8080

  backend:
    restart: always
    container_name: backend
    env_file:
      - ./DB.env
    build: 
      context: ./fastApi
    ports:
      - 8081:8081
    expose:
      - 8081
    depends_on:
      - db
    command: ['uvicorn', 'main:app', '--host=localhost', '--port=8081', '--reload']

  db:
    container_name: db
    image: postgres
    restart: always
    env_file:
      - ./DB.env
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - 5432:5432
    expose:
      - 5432

volumes:
  pgdata: